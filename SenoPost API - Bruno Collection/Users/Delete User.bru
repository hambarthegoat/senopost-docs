meta {
  name: Delete /me
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/users/me
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}

docs {
  Delete the current authenticated user's account using `DELETE /users/me`. Requires Authorization header.
}
