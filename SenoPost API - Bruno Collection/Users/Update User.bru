meta {
  name: Update /me
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/users/me
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "email": "changed@example.com"
  }
}

settings {
  encodeUrl: true
}

docs {
  Update the current authenticated user's profile. Uses `PATCH /users/me` and requires Authorization header.
}
