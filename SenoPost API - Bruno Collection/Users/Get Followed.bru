meta {
  name: Get Followed
  type: http
  seq: 5
}

get {
  url: {{base_url}}/followed
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Get list of followed communities for the authenticated user.
  
  **Response:**
  Returns an array of followed communities with id, name, and description.
  
  **Authentication:**
  Requires Bearer token.
}
