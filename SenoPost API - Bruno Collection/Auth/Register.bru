meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "User@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Register a new user. Returns created user id and email.
}
