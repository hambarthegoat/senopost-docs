meta {
  name: Vote on Post
  type: http
  seq: 1
}

post {
  url: {{base_url}}/posts/:id/votes
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "value": 1
  }
}

settings {
  encodeUrl: true
}

docs {
  Vote (value: 1 or -1) on a post. Requires auth.
}
