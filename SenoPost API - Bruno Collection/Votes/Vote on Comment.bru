meta {
  name: Vote on Comment
  type: http
  seq: 2
}

post {
  url: {{base_url}}/comments/:id/votes
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "value": -1
  }
}

settings {
  encodeUrl: true
}

docs {
  Vote (value: 1 or -1) on a comment. Requires auth.
}
