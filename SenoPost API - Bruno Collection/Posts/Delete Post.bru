meta {
  name: Delete Post
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/posts/:id
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}

docs {
  Delete post (requires auth).
}
