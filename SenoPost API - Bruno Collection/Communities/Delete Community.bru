meta {
  name: Delete Community
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/communities/:id
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}

docs {
  Delete community (requires auth). Returns 204-style response object.
  
  **Authorization:**
  Only the author (creator) of the community can delete it.
  Attempting to delete a community you didn't create will result in a 400 error.
}
